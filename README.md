# Task 3: Writing Basic SELECT Queries

## 📌 Objective
The goal of this task is to **extract data from one or more tables** in a relational database using basic SQL `SELECT` queries.  

By the end of this task, you will be able to:
- Retrieve all or specific columns from a table
- Filter data using `WHERE`, `AND`, `OR`, `LIKE`, and `BETWEEN`
- Sort data using `ORDER BY`
- Limit the number of rows returned with `LIMIT`

## 🛠️ Tools
-- MySQL Workbench**

## 📂 Database Used
The queries are written for the **Online Retail Application** database, which contains the following tables:
- customers
- categories
- products
- orders
- order_items
- payments
- reviews
